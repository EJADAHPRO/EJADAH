name: ejadah_server
description: Ejadah International Academy — Dart backend (modular monolith).
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.9.0

dependencies:
  # HTTP
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  # Database
  postgres: ^3.5.6
  # Security
  hashlib: ^1.22.0
  dart_jsonwebtoken: ^3.2.0
  crypto: ^3.0.6
  # Utilities
  collection: ^1.19.0
  meta: ^1.16.0
  intl: ^0.20.2

  ejadah_models:
    path: ../packages/ejadah_models

dev_dependencies:
  test: ^1.25.15
  lints: ^5.1.1
  http: ^1.3.0
